const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');
const root=path.join(__dirname,'..');const server=fs.readFileSync(path.join(root,'server.js'),'utf8');const utilities=fs.readFileSync(path.join(root,'utilities.js'),'utf8');const index=fs.readFileSync(path.join(root,'index.html'),'utf8');const resetHtml=fs.readFileSync(path.join(root,'reset-password.html'),'utf8');const resetJs=fs.readFileSync(path.join(root,'reset-password.js'),'utf8');
test('password recovery endpoints exist and use Supabase recovery',()=>{assert.match(server,/\/api\/auth\/password\/recover/);assert.match(server,/\/recover\?redirect_to=/);assert.match(server,/\/api\/auth\/password\/update/);assert.match(server,/body:\{password\}/)});
test('account drawer exposes forgot-password flow',()=>{assert.match(utilities,/Forgot your password\?/);assert.match(utilities,/renderAccount\('recover'\)/);assert.match(utilities,/\/api\/auth\/password\/recover/)});
test('Supabase recovery fragment is routed to dedicated reset page',()=>{assert.match(index,/type'\)===\'recovery\'/);assert.match(index,/reset-password\.html/);assert.match(resetHtml,/Choose a new password/);assert.match(resetJs,/access_token/);assert.match(resetJs,/\/api\/auth\/password\/update/)});
