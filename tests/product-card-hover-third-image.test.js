const test=require('node:test');const assert=require('node:assert/strict');const fs=require('node:fs');const path=require('node:path');const root=path.resolve(__dirname,'..');
test('PDP keeps free delivery threshold without UK restriction',()=>{const h=fs.readFileSync(path.join(root,'product.html'),'utf8');assert.match(h,/Free delivery on orders over £75/);assert.doesNotMatch(h,/Free UK delivery/i);});
test('all catalogue card surfaces use third product image for hover',()=>{for(const f of ['catalogue.js','collection-page.js','edit-page.js','product-page.js']){const s=fs.readFileSync(path.join(root,f),'utf8');assert.match(s,/images\?\.\[2\]/,f);assert.match(s,/card-product-image--hover/,f);}});
test('hover is desktop pointer-only and cover remains default',()=>{const s=fs.readFileSync(path.join(root,'styles.css'),'utf8');assert.match(s,/@media \(hover:hover\) and \(pointer:fine\)/);assert.match(s,/card-product-image--cover\{opacity:1/);assert.match(s,/card-product-image--hover\{opacity:0/);});
