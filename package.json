{
  "name": "averon-stripe-checkout",
  "version": "1.2.0",
  "private": true,
  "description": "AVERON storefront with Stripe Checkout, verified webhooks and local SQLite order persistence",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node server.js",
    "admin:hash": "node create-admin-hash.js",
    "test": "node --test tests/*.test.js",
    "deploy:verify": "node scripts/post-deploy-check.js"
  },
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "dotenv": "^16.4.7",
    "express": "^5.1.0",
    "helmet": "^8.1.0",
    "stripe": "^18.5.0"
  }
}
